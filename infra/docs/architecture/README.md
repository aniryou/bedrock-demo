# Architecture diagrams — order-triage AgentCore

**The combined [end-to-end lifecycle](end-to-end.md) hero (start here)** — all nine planes fused in
one frame (build `B1–B6` · request `R1–R7` · observe + a loop-back) — its detailed
[data-plane](data-plane.md) sibling, plus **six** code-grounded, orthogonal cross-sections of the
CUSTOM_JWT-authorized **AgentCore Runtime (`order_triage`)** running Strands over **Bedrock Nova
Lite** (reasoning, knowledge, security, memory, observability, evaluation), plus a **delivery plane**
(#7) showing how a merge to `main` reaches the live stack. All nine are **AWS-style SVGs** (official
AWS icons, left → right) generated by the **`architecture-skill`** skill from [`specs.json`](specs.json)
— the editable source of truth (the renderer is not vendored here; the skill regenerates the diagrams
from the spec). Every box traces to real code/IaC across the mono-repo (`agent`, `knowledge`, `stubs`,
`app`, `infra`); each plane lists its `## Provenance` so a reader can jump from a box to the file behind it.

Each plane also ships as an **editable [`draw.io`](https://app.diagrams.net) file**
(`<plane>-architecture.drawio`), plus one combined multi-tab [`architecture.drawio`](architecture.drawio)
holding all nine — same geometry and icons as the SVGs, but with every box a draggable, recolourable,
reconnectable shape (icons embedded, so the file is self-contained). Open in draw.io desktop,
app.diagrams.net, or the VS Code "Draw.io Integration" extension. The **SVG stays the canonical
rendered view** (it's what these `.md` pages embed); the `.drawio` is for hand-editing / reuse. Both
are generated from `specs.json` — edit the spec and re-run the skill; don't hand-edit the SVG (the
`.drawio` you *may* tweak by hand, but a regenerate will overwrite it).

The first is the whole system in one frame; the second its detailed request plane; read the rest as orthogonal cross-sections:

| # | Diagram | Plane it shows | Primary sources | Decision record |
|---|---|---|---|---|
| 0 | [End-to-end lifecycle](end-to-end.md) | **The whole lifecycle in one frame** — all nine planes fused: build `B1–B6` · request `R1–R7` · observe + a loop-back; start here | all of the below | — |
| 0·detail | [Detailed data plane](data-plane.md) | **The whole flow** — the request spine (`R1–R7`) of the hero at wire level: expands the identity / Cedar / OBO / RLS chain, with a finer-grained Mermaid wire-level view | `runtime.tf`/`gateway.tf`/`policy.tf`/`*_lambda.tf`, agent `runtime.py` + `agent_kit` (`infra.gateway`/`infra.identity`), `stubs/snowflake_stub` | [ADR-0001](../adr/0001-user-impersonation-obo.md), [ADR-0008](../adr/0008-semantic-view-cortex-analyst.md) |
| 1 | [Agent Architecture](agent-architecture.md) | **Reasoning plane** — system-prompt assembly, the model⇄tools event loop, LOCAL vs Gateway/MCP tools, the three knowledge surfaces (ontology · skills · KB) | `agent_kit.*` (consumed by `agent/src/order_triage/`), `knowledge/{ontology,kb,skills}`, infra `runtime.tf`/`gateway.tf`/`knowledge_base.tf` | [data-plane.md](data-plane.md) |
| 2 | [Knowledge Architecture](knowledge-architecture.md) | **Knowledge plane** (structural companion to #1) — the enterprise ontology (Objects + Actions), Skills and KB; how assets cross-reference by `apiName` and compile to the `bindings.json` reverse index; the 3 LOCAL knowledge tools and the one Action→Gateway-tool binding (the agent's `ACTIONS` map) | `knowledge/{ontology,skills,kb}`, `agent_kit.knowledge.{ontology,skills,kb,coverage}`, infra `knowledge_base.tf`/`gateway.tf`/`policy.tf` | [ADR-0001](../adr/0001-user-impersonation-obo.md), [ADR-0008](../adr/0008-semantic-view-cortex-analyst.md) |
| 3 | [Security Architecture](security-architecture.md) | **Identity / authorization plane** — Entra inbound CUSTOM_JWT, Cedar at the Gateway, the Guardrail, the agent-authority `SigV4` vs Entra-OBO `TOKEN_EXCHANGE` egress split, Snowflake `EXTERNAL_OAUTH` + RLS | infra `identity.tf`/`policy.tf`/`guardrail.tf`, `agent_kit.infra.{identity,gateway}`, `stubs/snowflake_stub`, `app/app/entra.py` | [ADR-0001](../adr/0001-user-impersonation-obo.md), [ADR-0003](../adr/0003-bedrock-guardrail.md), [ADR-0006](../adr/0006-gateway-role-least-privilege.md) |
| 4 | [Memory Architecture](memory-architecture.md) | **Per-user state plane** — `actor_id`-keyed short-term events + the SEMANTIC / USER_PREFERENCE / SUMMARIZATION long-term strategies, the async write path vs the per-turn `<user_context>` read path | infra `memory.tf`, `agent_kit.infra.{memory,identity}` | [ADR-0002](../adr/0002-agentcore-memory-activation.md) |
| 5 | [Observability Architecture](observability-architecture.md) | **Control plane** that the other four emit into — ADOT `gen_ai.*` spans → X-Ray Transaction Search, the EMF token metric, vended logs + PII mask, dashboards / alarms / SLOs / Contributor Insights | infra `terraform/modules/observability/*`, `agent_kit.infra.metrics` (`emit_usage_metric`) | [ADR-0004](../adr/0004-observability-finops.md) |
| 6 | [Evaluation Architecture](evaluation-architecture.md) | **Quality loop** — the offline pytest judge that gates the image, and AgentCore Online Evaluations grading sampled live `gen_ai` spans | infra `modules/observability/evaluations.tf`, agent `evals/` | [ADR-0005](../adr/0005-online-evaluations.md) |
| 7 | [DevOps / CI-CD](devops-architecture.md) | **Delivery plane** — the mono-repo's path-filtered pipeline: a merge routes by folder to per-folder CI + the `agent-build` / `stubs-release` publishers (ECR image · S3 zips/KB), which `repository_dispatch` into a human-gated, OIDC-auth `terraform apply` | `.github/workflows/*`, `infra/bootstrap/github_oidc.tf`, `deploy.yml` | [cd-setup.md](../playbooks/cd-setup.md) |

## How they relate

These diagrams decompose a single system along one shared spine. **Agent** is the reasoning
plane (how it thinks and acts); **Knowledge** is its structural companion — the shape of the
enterprise ontology / skills / KB corpus the reasoning plane consults, and the single
`ACTIONS` seam (the agent's action map in `agent.py`) where an ontology Action binds to a Gateway tool; **Security** is the
identity/authorization plane wrapped around
that reasoning (who is allowed to do what, *as whom* — the agent-authority `SigV4` vs Entra-OBO
Snowflake split is the heart of the design); **Memory** is the per-user state plane keyed by the
*same* Entra subject that drives OBO; **Observability** is the grey control plane that the other
runtime planes emit telemetry into (ADOT `gen_ai` spans to X-Ray Transaction Search, the per-turn EMF
token metric, the audience dashboards and alarms); and **Evaluation** closes the loop by grading
the runtime's `gen_ai` spans both offline (the CI gate that blocks the agent image) and online
(sampled live traces via AgentCore Online Evaluations).

The connective tissue kept identical across the planes: the **AgentCore Runtime (`order_triage`)**
node, **Bedrock Nova Lite** (`amazon.nova-lite-v1:0`), the **AgentCore Gateway (MCP)**, **Cedar
Policy Engine (ENFORCE)**, **AgentCore Memory**, the Entra app set (`api://agent-app`,
`api://order-triage-snowflake`, the `entra-obo` provider), the **snowflake-query Lambda**, and
the `gen_ai`/`aws/spans` telemetry that Observability and Evaluation share.

## Scope convention

The **[end-to-end lifecycle](end-to-end.md)** hero is the deliberate exception — it fuses all of
these into one frame. Every *cross-section* diagram, by contrast, states what it omits and defers the
**live request / data plane** — the end-to-end flow from the Entra-authenticated caller through the
Gateway, the stub Lambdas, OBO, and into Snowflake as the calling human — to the canonical
[`data-plane.md`](data-plane.md). The build / publish / deploy pipeline (path-filtered GitHub
Actions, OIDC, ECR/S3 artifacts) that the request-time planes omit is its own plane —
**[DevOps / CI-CD](devops-architecture.md)** (#7) — distilled from `.github/workflows/*` and
[`playbooks/cd-setup.md`](../playbooks/cd-setup.md).

## Visual grammar (shared by all nine)

- **Official AWS icons**, laid out **left → right**. Each box is a subsystem; its sub-line names
  the parts (e.g. *AgentCore Gateway · Cedar ENFORCE · OBO broker*).
- **Edges** — **solid dark** = request / data path · **blue dashed** = identity / token / secret ·
  **grey** = supporting (incl. telemetry). Primary steps are numbered (`a/b/c` for a parallel
  fan-out, e.g. the `8a/8b/8c` SigV4-vs-OBO egress split in Security).
- **Rounded zones** group by trust / responsibility — a solid *AWS Cloud · us-west-2* boundary,
  dashed sub-zones (*AWS Lambda · Function URLs*), and the *Observability* control plane.

> Generated, not hand-drawn: each diagram is `<plane>-architecture.svg`, emitted from
> [`specs.json`](specs.json) by the **`architecture-skill`** skill (which owns the
> `generate.py` + `_awsviz.py` renderer — no longer vendored in this repo). Run the skill on
> `specs.json` to regenerate. The official AWS icon PNGs come from the `diagrams` package
> (base64-embedded so GitHub renders the SVG inline); PNG fallbacks need `rsvg-convert`
> (`brew install librsvg`). The node / edge / group grammar lives in the skill's `_awsviz.py`.
> **Edit the spec, never the SVG.**
