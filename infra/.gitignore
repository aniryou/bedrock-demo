# secrets
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ruff_cache/

# terraform (keep .terraform.lock.hcl committed)
**/.terraform/
*.tfstate
*.tfstate.*
terraform/terraform.tfvars
*.auto.tfvars
crash.log
.DS_Store

# snowflake bootstrap — generated service-user key-pair, never commit
snowflake/.keys/

# actor-resolver Lambda zip — generated by archive_file at plan time
terraform/modules/observability/lambda/*.zip
