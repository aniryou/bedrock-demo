# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS / editors
.DS_Store
.idea/
.vscode/

# Generated reports — regenerated by the build/*.py scripts and uploaded by CI on
# every run, so no need to commit them.
build/lineage.md
build/lineage.csv
build/bindings.md
build/ontology.html
build/ontology-summary.md

# NOTE: build/ontology.compiled.json and build/bindings.json are intentionally NOT
# ignored. They are the artifacts the agent consumes, and CI gates on them being
# committed and current (the "up to date" drift checks). Don't add them here.
